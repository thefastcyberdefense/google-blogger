# Phase 2B PR B: content discovery

Approved 2026-09-09: maximum 28-file manifest (21 existing/7 new), one branch feat/fcd-content-discovery, one draft PR, raw JS growth <=8192 bytes and CSS <=2048 against base 18b127d014e5d02a860603b18f10dd6c7ab81169, XML <=500000. Verified main run 34338858565 passed. Branch-only source-bound XML transfer is approved; no merge, deployment, deletion or publishing.

Use FCD Superpowers, Ralph and GSD, plus accessibility and code review as applicable. All automated project execution in Actions. No claim of native CLI/background-agent installation.

## Story ledger

B1 normalized contract: red baseline being established. B2 model/identity pending. B3 shared bounded transport/retry pending. B4 ranking pending. B5 native post-only UI and Recent Posts pending. B6 clear filter pending. B7 full review/evidence pending. B8 XML transfer and final read-only verification pending.

Source preview acceptance B-01 through B-29 remains mandatory. Preserve inherited 129 unit/contract and 792 browser regressions. Related results max3, Recent Posts max5; post request max50, catalog max8; 500000 decoded bytes and 8 seconds per attempt; one automatic attempt plus one shared explicit retry. No persistent cache, feed pagination, recommendation images, new dependencies or external search service. Native search remains authoritative for the full publication.

Candidate labels normalize NFKC/whitespace/case; rank shared distinct labels, then valid publication date, then stable identity. Exclude current post by ID or normalized URL. Strip only known mobile/tracking variants for comparison, not meaningful query parameters/path case. Preserve native fallback navigation and private/disabled/redirected feed limitations. Native Blogger import/save and human accessibility remain separate pending release gates.
