export type MetadataInput={html:string;view:string;url:string};
// Test-first boundary. Not wired into staging until behavior is verified.
export function validateMetadata(_input:MetadataInput):string[]{return [];}
